file <- read.csv("episode_fuel.csv",header=FALSE)
t <- aggregate(file,by=list(V1,V2),FUN="mean)
)
t <- aggregate(file,by=list(V1,V2),FUN="mean")
t <- aggregate(file,by=list(file$V1,file$V2),FUN="mean")
t <- aggregate(file,by=list(file['V1'],file['V2']),FUN="mean")
t <- aggregate(V3~V2+V1,file,mean)
t
t <- aggregate(V3~V2+V1,file,mean)
q()
